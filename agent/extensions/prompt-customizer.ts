import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function promptCustomizer(_pi: ExtensionAPI) {
	// Disabled for now. Keep the file as a placeholder so the implementation can
	// be revisited later without re-discovering the previous approach.
}
