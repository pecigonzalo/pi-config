{
  "name": "session-reminder",
  "scripts": {
    "test": "bun test --dots --only-failures"
  },
  "pi": {
    "extensions": [
      "./session-reminder.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
