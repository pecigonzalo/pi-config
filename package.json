{
  "workspaces": [
    "agent/extensions/*"
  ],
  "scripts": {
    "generate:footer-schema": "cd agent/extensions/footer && bun run generate:footer-schema",
    "prepare": "lefthook install",
    "test": "bun run --filter './agent/extensions/*' test"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.75.1",
    "@types/bun": "^1.3.14",
    "lefthook": "^2.1.6"
  }
}
